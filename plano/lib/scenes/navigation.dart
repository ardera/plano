/*

  Navigation Scene
  ----------------

*/

import 'package:flutter/material.dart';

class NavigationScene extends StatelessWidget {
  const NavigationScene({super.key});

  @override
  Widget build(BuildContext context) {
    return const ColoredBox(
      color: Colors.pink,
      child: Center(
        child: Text('Navigation Scene'),
      ),
    );
  }
}
